import { Typography, styled } from "@mui/material";

/**
 * Instructions text for ForgotPassword page
 * Consistent typography styling
 */
export const StyledInstructionsText = styled(Typography)({
  fontFamily: "'General Sans', sans-serif",
  fontWeight: 500,
  fontSize: "14px",
  color: "#565656",
  lineHeight: 1.5,
});
