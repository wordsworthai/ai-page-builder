import { Box, styled } from "@mui/material";

/**
 * Container for "Forgot password?" link
 * Centered flex container
 */
export const StyledForgotPasswordLinkContainer = styled(Box)({
  display: "flex",
  justifyContent: "center",
});
